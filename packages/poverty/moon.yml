tasks:
  install:
    command: go mod download && go install github.com/air-verse/air@latest
  build:
    command: go build -o bin/server ./cmd/server
    outputs:
      - bin/server
  start:
    command: ./bin/server
  dev:
    command: air
