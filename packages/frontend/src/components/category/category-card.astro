---
export type Props = {
  name: string;
  slug: string;
};

const { name, slug } = Astro.props;
---

<a href={slug} class="text-beige-300 font-sans font-semibold">
  <div class="rounded-lg p-4 shadow-lg">
    <h3 class="text-2xl">{name}</h3>
  </div>
</a>
